# TODO: pin to a hydra-genetics PyPI release once one with Snakemake 9 / Python 3.12
# support is published. Until then install the migration branch directly.
hydra-genetics @ git+https://github.com/hydra-genetics/hydra-genetics.git@migrate-to-snakemake9-python3.12
jinja2>=3.1.0
numpy>=1.24.0
pandas>=1.3.1
pyaml-env>=1.2.2
snakemake>=9.0.0,<10
tabulate<0.9.0
